import { StyleSheet } from "react-native";

export const globalStyle = StyleSheet.create({
    container: {
        padding: 30
    },
    titleText: {
        fontFamily: 'roboto-bold',
        fontSize: 18
    }
}) 